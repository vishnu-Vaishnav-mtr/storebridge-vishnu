import { AuthPage } from "@/components/auth-page";

export default function RegisterPage() {
  return (
    <AuthPage
      title="Create your account"
      action="Create account"
      footer="Already registered? Log in"
    />
  );
}
